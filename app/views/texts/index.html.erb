<section class="main-header">
  <%= image_tag("texts.png") %>
</section>
<section class="about">
  <h1><b>上から順に進めていきましょう</b></h1>
  <p><b>テキストは少しづつ増やしていきますので、ゆっくり進めてください</b></p>
</section>
<section class="menu">
  <% @texts.each do |text| %>
    <%= link_to text.title, text, class: "btn btn-info btn btn-block" %>
  <% end %>
  <%= link_to 'New Text', new_text_path, class: "btn btn-success btn btn-block" if user_signed_in? && current_user.role == "admin" %>
</section>
